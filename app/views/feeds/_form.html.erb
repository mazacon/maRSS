<%= form_with(model: feed) do |form| %>
  <% if feed.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(feed.errors.count, "error") %> prohibited this feed from being saved:</h2>

      <ul>
        <% feed.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label 'link:', style: "display: block" %>
    <%= form.text_field :link %>
  </div>


  <div>
    <%= form.label :user_id, style: "display: none" %>
    <%= form.text_field :user_id, value: session[:user_id], style: "display: none" %>
  </div>

  <div style="padding-top: 12px; padding-bottom: 0px;">
    <%= form.submit 'Create feed', class: 'btn btn-primary' %>
  </div>
<% end %>
